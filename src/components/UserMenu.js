import StyledUserMenu from "../styles/StyledUserMenu";

const UserMenu = () => {
  return (
    <StyledUserMenu>
      <div>Profile</div>
      <div>Users</div>
      <div>Logout</div>
    </StyledUserMenu>
  );
};

export default UserMenu;
